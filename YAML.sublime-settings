// YAML Preferences – ~lojojaPrefs
// ================================================================
{
  // File extensions to associate with this language
  "extensions": [
    "yaml",
    "yml"
  ],

  // The number of spaces a tab is considered equal to
  "tab_size": 2
}
